name: basin-deploy

project_files:
  - web-build/Dockerfile.basin-deploy
  - commands/web/basin-deploy
  - ansible/tasks/before-cleanup-tasks.yml
  - ansible/rsync-exclude

ddev_version_constraint: '>= v1.25.1'
